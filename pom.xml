<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 1. It still inherits from Spring Boot, like your app does today -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <!-- 2. Identity of the AGGREGATOR project (not an app) -->
    <groupId>com.artisttools</groupId>
    <artifactId>artist-copilot</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>            <!-- KEY: builds no jar, just coordinates -->
    <name>artist-copilot (parent)</name>

    <!-- 3. The list of services. This is the heart of the parent. -->
    <modules>
        <module>profile-gateway</module>
        <module>image-service</module>
        <module>profile-service</module>
        <module>venue-service</module>
        <module>profile-gateway</module>
    </modules>

    <!-- 4. Shared properties every child inherits -->
    <properties>
        <java.version>25</java.version>
        <spring-cloud.version>2025.1.2</spring-cloud.version>
    </properties>

    <!-- 5. Shared dependency VERSIONS (not actual deps) -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 6. Shared build plugins (e.g. the Boot image build you already use) -->
    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <image>
                            <name>xavierhaurie2/${project.artifactId}:${project.version}</name>
                        </image>
                        <pullPolicy>IF_NOT_PRESENT</pullPolicy>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
